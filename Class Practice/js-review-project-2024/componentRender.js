(
    ()=>
        ()=>{[Table ,SideBar].map(component => new component().render())}
)
()
();

// [Table ,SideBar].map(component => new component().render());

// Explain difference between above 2 lines